import React from 'react';
import ReactDOM from 'react-dom/client';
import NomNomApp from './NomNomApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <NomNomApp />
  </React.StrictMode>
);
